# Invited talks and presentations, shown newest-first by `date`.
# `date` must be ISO format (YYYY-MM-DD). `url` may link to slides or a recording.
# Validated against schemas/talks.schema.json.

items: null
